  app-dev:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./server:/app/server
      - ./overlay:/app/overlay
      - ./data:/app/data
    command: ["node", "server/src/index.js"]
    restart: unless-stopped
