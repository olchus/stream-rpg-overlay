services:
  app:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - STREAMLABS_SOCKET_TOKEN=${STREAMLABS_SOCKET_TOKEN}
      - KICK_CHANNEL=${KICK_CHANNEL}
    restart: unless-stopped